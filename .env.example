# Supabase 连接配置 —— 复制本文件为 .env.local，并填入真实值
#
# .env.local 已被 .gitignore 排除，不会进仓库。
#
# 取值位置：Supabase 控制台 → 选择项目 → Settings → API
#   VITE_SUPABASE_URL      → Project URL，形如 https://xxxxxxxx.supabase.co
#   VITE_SUPABASE_ANON_KEY → Publishable key（sb_publishable_...）
#                            或旧版 anon key（eyJ... 开头的 JWT），二者等效
#
# ⚠️ 绝对不要把 SECRET key / service_role key 填到这里。
#    那个密钥会绕过全部 RLS 行级安全策略，只能存在于服务端（如 Edge Function），
#    一旦打包进前端就等于把一个无密码的数据库后台挂上公网。
#
# 注：Vite 只把 VITE_ 前缀的变量注入前端代码。
#     若后续改用 Next.js，需改名为 NEXT_PUBLIC_ 前缀。
#
# ---------------------------------------------------------------------------
# AI 深度分析（DeepSeek）—— **这里没有它的变量，是故意的**
# ---------------------------------------------------------------------------
# 这功能用到的密钥放在 **Supabase 控制台的 Edge Functions → Secrets** 里，
# 由 supabase/functions/ai-analysis/index.ts 通过 Deno.env.get() 读取：
#
#   DEEPSEEK_API_KEY    DeepSeek 的 API key
#   DEEPSEEK_MODEL      模型名，默认 deepseek-flash
#   DEEPSEEK_THINKING   disabled（默认）/ enabled
#
# ⚠️ **绝对不要在这里加 VITE_DEEPSEEK_API_KEY。**
#    和上面那条 service_role 的警告是同一个道理：线上是静态站点（GitHub
#    Pages），Vite 会把 VITE_ 变量的**字面值**打进公开的 JS 产物，谁都能
#    拿走拿去刷。密钥必须留在服务端。
#
#    这也是为什么浏览器不直接调 api.deepseek.com，而是经由那个 Edge
#    Function 转一手。

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
