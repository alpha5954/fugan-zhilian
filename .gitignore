# dependencies
node_modules/
.pnp
.pnp.js

# build output
dist/
dist-ssr/
build/
*.local

# env — Supabase 密钥走这里，不要提交
.env
.env.local
.env.*.local

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
logs/
*.log

# editor / os
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db
desktop.ini

# 临时抽取的计划书副本
_attached.pdf
_attached.txt

# 本地验证时留下的截图与临时脚本。
# 只锚定仓库根目录 —— public/ 里的图标（favicon-*.png / logo.png / apple-touch-icon.png）
# 是产物的一部分，必须提交，不能跟着一起被忽略。
/*.png
/*.mjs
